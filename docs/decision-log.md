# 결정 검증·변경 이력

확정된 결정을 확인한 결과를 append-only로 남긴다. 규칙은
[`L-1-operating-principles.md`](L-1-operating-principles.md) 6절, 확정 여부 판정은
[`L0-project-context.md`](L0-project-context.md) 4절을 따른다.

**결과가 기존 결정을 지지해도 남긴다.** "문제없음"은 그 결정을 계속 믿어도 되는 근거이며,
이 기록이 없으면 몇 주 뒤에 같은 조사를 다시 하게 된다.

`L0` 4절에는 **현재 상태만** 두고, 왜 그렇게 됐는지는 여기에 쌓는다. 기존 줄은 고치지 않고
아래에 추가한다.

| 날짜 | 대상 결정 | 확인한 내용 | 결과 | 영향 |
| --- | --- | --- | --- | --- |
| 2026-08-19 | 매칭 키 (L0 §5) | 주차장 CSV 도로명주소 채움률 | 29.8% | 신규 — "도로명을 공통 키로 신뢰하기 어렵다" 제약 생성 |
| 2026-08-20 | 매칭 키 (L0 §5) | 일반음식점·휴게음식점 도로명주소 채움률 | 99.3% / 99.4% | **적용 범위 깨짐.** "모든 원천에서 부족"은 주차장 하나를 일반화한 것이었다. 지번은 두 원천 모두 98.6~100%로 공통 키 유효. 제약을 `매칭 키 부족` → `매칭 키 편차`로 정정하고, 도로명은 양쪽 원천에 있을 때만 보조 키로 쓰도록 범위 축소 |
| 2026-08-20 | 장소당 SUBTYPE 다중 연결 | `tour_api_subtypes.csv` 239행에서 동일 `(contentType, 분류코드)`에 SUBTYPE이 2개 이상 붙는 경우 | **0건.** DB도 `place_categories` 49,581 = `places` 49,581로 1:1 | **기존 결정 유지.** 다중 subtype 적용 범위 판정 규칙을 지금 만들 필요 없음. README가 다중 연결을 허용하므로 실제 사례가 생기면 그때 판단 |
| 2026-08-20 | `InfoKey` 적용 범위 3단계 (L0 §4) | 1차 `InfoKey` 22개가 COMMON / PLACE_TYPE / PLACE_SUBTYPE 중 하나로 떨어지는지, 혼합이 필요한 키가 있는지 | 22개 전부 단일 축으로 분류됨. 혼합 불필요 | **기존 결정 유지.** `applicability_scope` 3값으로 충분. 원천이 늘어 혼합 사례가 생기면 재검토 |
| 2026-08-20 | TourAPI의 역할 (other-source-survey 1절) | TourAPI가 일상 모드의 모집단이 되는지 — 부울경 기준 인허가 원천과 건수 대조 | RESTAURANT 1,863 vs 106,186 (57배), CAFE 약 440 vs 13,987 (32배), BAR **0** vs 13,229 | **전제 확인됨.** "TourAPI는 전체 기준선이 아니라 관광 콘텐츠 원천"이라는 기존 판단이 실측으로 뒷받침됨. base 원천을 인허가로 확정 |
| 2026-08-20 | 좌표계 혼재 (L0 §5) | 주차장 표준데이터와 식품 인허가의 좌표 범위 | 주차장 **WGS84** (18,117/18,879이 한반도 범위), 인허가 **EPSG:5174 계열** (X 364,928~407,758) | **적용 범위 명확화.** "인허가 API 계열은 EPSG:5174"는 유지되나 표준데이터는 원천별로 다름. 주차장은 변환 불필요 |
| 2026-08-20 | 트래픽 증설 (GO-67) | 공공데이터포털 운영계정 전환 요건 | 활용사례 등록 필수 — 서비스 URL·캡처 화면·개시일. 출시 전이라 충족 불가 | **외부 제약으로 결정 변경.** 증설 포기, 수집 범위를 부울경으로 축소(`GO-111`). 실측이 아니라 외부 계약 확인이 근거 |
| 2026-08-20 | 매칭 키 편차 (L0 §5) | 정정 문구 자체를 재검토 — "지번주소는 원천 공통으로 채움률이 높다"의 근거 범위 | 실제 측정한 원천은 주차장·일반음식점·휴게음식점 3개뿐 | **같은 과일반화 반복.** 한 턴 전에 정정한 패턴을 다시 썼다. "원천 공통으로" → "현재 확인한 두 원천에서는"으로 범위 한정. 원천 추가 시 채움률 재실측 문구 추가 |
| 2026-08-20 | L0 §5 구성 | §5에 확정된 제약·조사 필요·결정 보류가 섞여 있어 읽는 사람이 행동을 구분할 수 없는 문제 | 11개 행 중 확정 8 / 조사 2 / 보류 1 | **구조 변경.** `구분` 열과 `후속` 열 추가. 조사·보류 항목은 후속 이슈 없이 방치돼 있었으므로 `GO-121` 이하 3건을 신설해 연결 |
| 2026-08-20 | L0 §4 확정 판정 권한 | §4를 "확정 여부를 판정하는 곳은 여기뿐"으로 배타화한 뒤 다른 절과의 정합성 | §7이 여전히 Jira를 "확정된 결정"의 근거로 기술 | **문서 내 모순.** §7을 "작업 이력·의존 순서·미결 항목·결정 근거"로 고치고 확정 판정 권한이 §4에 있음을 명시. L0 §4 / decision-log / Jira의 역할이 갈림 |
| 2026-08-20 | 인허가 갱신시점 항목명 | 항목명이 내용과 맞는지 | 47% 몰림은 이미 측정 완료. 미확정인 것은 증분 커서 사용 가능 여부 | **항목명 정정.** `신뢰성 미검증` → `증분 기준 적합성 미확정`. 측정한 것과 판단이 남은 것을 구분 |
| 2026-08-20 | L0 §5 성격 | §5에 절차 문장("원천 추가 시 채움률 실측")이 섞인 문제 | §4 원천 채택 절차가 이미 채움률 실측을 규정 | **중복 제거.** §5는 확인된 사실만 남기고, 절차는 §4로 이관. 다만 주소 채움률을 빠뜨리지 않도록 §4에 "지번·도로명주소 반드시 포함"을 명시 |
| 2026-08-20 | L0 §4 `원천 역할 분담` | 새로 넣은 `원천 선택 단위`("실측 전에는 후보")와 기존 행의 정합성 | 역할 분담 행이 상가정보 등 미실측 원천을 확정형으로 서술 | **행 제거.** 역할 분담은 규칙이 아니라 규칙의 적용 결과이므로 survey 1절 표가 정본. L0에 원천 이름을 하드코딩하면 결론이 바뀔 때마다 L0를 고쳐야 한다 |
| 2026-08-20 | L0 §4 `우선 원천` | 새 `원천 선택 단위` 행과 층위가 겹치는지 | 선택 방법(어느 원천을 쓸지)과 사용 방법(채택 후 값 고르기)이 한 이름에 섞임 | **이름·문구 변경.** `채택 후 값 선택 규칙`으로 바꾸고, 우선순위 소유를 survey 1절로 명시 |
| 2026-08-20 | survey 1절 사실별 원천 표 | 표의 `상태` 열이 실제 실측 진행과 일치하는지 | 8행 전부 `미실측`이나 주차장·식품 인허가는 오늘 실측 완료 | **표 갱신.** `실측 완료` / `부분 실측` / `미실측`로 구분하고 `근거` 열에 Jira 키 연결. 부분 실측은 무엇이 남았는지 명시 |
| 2026-08-20 | 원천 선택 단위 (L0 §4) | 카페가 인허가·상가정보 양쪽에서 나올 때 어느 쪽을 모집단으로 쓸지 — 부산 카페 5,179 vs 7,335 중복률 실측 | 상가정보 단독 3,151건 중 33.7%는 인허가에 폐업으로 존재, 66.3%는 프랜차이즈 매칭 실패(주소 표기 차이) | **기존 결정 지지.** 상가정보의 4,290건 초과는 신규가 아니라 폐업 + 매칭 실패였다. 모집단은 인허가(영업 상태 제공), 상가정보는 업종·층/호 보강. 사실별 선택 규칙이 실측으로 확인됨 |
| 2026-08-20 | 다중 원천 병합 (L0 §4) | 지번주소 기반 자동 매칭이 실사용 가능한 수준인지 | 자동 병합 가능 74.4%, 한 주소 2건 이상 19.0% | **기준선 확보, 결정 변경 없음.** "모호한 후보는 병합하지 않는다"에 따라 19%는 병합 제외. 74.4%는 개선 대상이지 채택 기준이 아니며, 지번 표기 정규화 보강은 P5 작업 |
| 2026-08-20 | ACCOMMODATION base | 숙박업·관광숙박업 인허가와 상가정보 숙박 대분류 비교 | 인허가 부울경 6,385건(영업 상태 있음), 상가정보 11,189건(영업 상태 없음) | **base 확정.** 모집단은 인허가, 상가정보는 업종 보강. 카페에서 확인한 패턴(상가정보 초과분 = 폐업 + 매칭 실패)을 적용. 업태명이 MOTEL·HOTEL·RESORT_CONDO에 그대로 대응 |
| 2026-08-20 | base 원천 5종 | RESTAURANT·BAR·CAFE·SHOP·ACCOMMODATION의 모집단 원천 | 5개 전부 실측으로 확정. TOURIST_SPOT·ACTIVITY는 TourAPI, PARKING_LOT은 주차장 표준데이터로 유일 원천 | **P1 base 결정 완료.** survey 1절 표에서 인허가·상가정보를 `미실측`에서 `채택`으로 승격 |
| 2026-08-20 | survey 1절 원천 상태 | 인허가·상가정보를 `채택`으로 올린 것이 타당한지 재검토 | 인허가의 핵심 기준은 권위성·최신성인데 **최신성을 재지 않았다**(`GO-123` 미완). 상가정보도 상가업소번호 안정성·중복률 미측정 | **승격 철회.** `채택` → `부분 실측`으로 되돌림. base 결정(어느 원천으로 Place를 만들지)과 원천 채택(그 사실의 정본 확정)을 섞은 것이 원인. survey에 두 개념의 구분을 명시 |
| 2026-08-20 | 검증 기록 방식 | 하루 동안 같은 종류의 실수가 세 번 난 원인 분석 — 도로명 과일반화, "원천 공통으로" 재발, base→채택 성급한 승격 | 셋 다 **측정 범위를 넘어 결론을 낸 것**. 세 번 모두 사용자가 잡았고 문서가 잡지 못함. 기존 장치는 사후 기록이지 사전 차단이 아님 | **규칙 추가.** L-1 §6에 "결론에는 측정 범위를 함께 적는다. 범위 없는 결론은 기록하지 않는다" 신설. survey `상태` 칸에도 동일 적용 |
| 2026-08-20 | GO-66 표본 대상 | base 원천 확정 후 TourAPI 상세 표본을 음식점(39)으로 뜨는 것이 유효한지 | TourAPI가 base로 남는 것은 TOURIST_SPOT·ACTIVITY뿐. 음식점 상세를 떠도 쓸 곳이 없음 | **표본 대상 변경.** 음식점(39) → 관광지(12), 부울경 1,920건. GO-70 제목·설명도 변경. 완료 조건과 나머지 하위 작업은 유지 [측정: GO-120의 6개 원천 부울경 건수 비교. TourAPI 상세 응답 자체는 미실측] |
| 2026-08-20 | attribute_sources.csv 채움률 | 관광지 5개 항목의 명세 추정치를 실측 검증 [측정: 관광지(12) 부울경 200건 층화표본, seed 20260820. 문화시설·축제·레포츠 미측정] | BUSINESS_HOURS 97%→96.5% 일치, PARKING_CONVENIENCE 75%→93.0% 상향, **PRICE_LEVEL·ENTRANCE_FEE_LEVEL·VISIT_DURATION은 13%·2%→0%** (필드 자체가 응답에 없음) | **csv 갱신.** `measured_fill_rate`·`measured_scope`·`measured_note` 열 추가. 추정치는 남기고 실측을 따로 기록해 범위가 드러나게 함. 실측 0%인 3개는 `NO_SOURCE_MEASURED`로 변경 |
| 2026-08-20 | 표본 층 기준 | 지역·시군구·분류로 층을 잡는 것이 적절한지 | 관광지 1,920건이 168개 층으로 갈라져 표본 200건 중 143개 층이 1건씩 차지. 모집단 77건 층과 1건 층이 같은 무게가 됨 | **층 기준 변경.** 시군구를 빼고 지역·분류 12개 층으로. 전체 채움률 추정이 작은 층으로 치우치는 것을 막음. 시군구 값은 보존 |
| 2026-08-20 | 레포츠 응답 실패율 (attribute-coverage) | "레포츠 30건 중 14건만 응답이 돌아왔다"는 기존 해석 검증 [측정: 레포츠(28) 부울경 200건 층화표본] | TourAPI는 `resultCode 0000`으로 정상 응답하며 `items`만 비어 있다. 수집 실패 0건, **상세 미등록 118건(59%)** | **해석 정정.** 응답 실패가 아니라 원천에 상세가 없는 것이다. L0 §2-3 기준으로 PARSE_FAILED가 아니라 MISSING. 수집기가 이를 오류로 보고 3회씩 재시도해 354회를 낭비하고 있었다 |
| 2026-08-20 | ACTIVITY 실효 확보율 | 기록된 BUSINESS_HOURS 93% / PARKING_CONVENIENCE 7%의 의미 | 상세 존재 82건 기준으로는 96.3% / 9.8%로 기록과 유사. 그러나 **모집단 대비 실효 확보율은 약 39% / 4%** | **분모 명시.** 상세 미등록률을 곱해야 실제 확보율이 나온다. 관광지(상세 존재율 100%)와 레포츠(41%)는 같은 채움률이어도 결과가 다르다. csv `measured_note`에 실효 확보율을 함께 기록 |
| 2026-08-20 | 채움률 분모 계산 | 수집기의 `fill_rates()`가 빈 응답을 분모에서 제외하는지 | `fetched`와 `empty`가 같은 건을 중복 계상. 레포츠 채움률이 절반으로 왜곡됨 | **버그 수정.** 분모를 `fetched + reused - empty`로 변경. 테스트 작성 중 발견됐고, 테스트가 없었으면 잘못된 수치를 기록할 뻔했다 |
| 2026-08-20 | AttributeKey 4축 분류 (GO-55 1차) | GO-36 경계 확정 후 1차 분류(① 13 / ② 4 / ③ 8 / ④ 9)가 유지되는지 [측정: 관광지·레포츠 각 200건 표본, attribute_sources.csv 69조합. 음식점·카페·숙소 상세 미측정] | 축 이동 없음. 34개 합계 일치 | **기존 분류 지지.** 최종 처리로 확정 — KEEP_ATTRIBUTE 13 / SPLIT_INFO_ATTRIBUTE 4 / MOVE_TO_PURPOSE 8 / REDESIGN 9 / MOVE_TO_INFO 0. Attribute 유지는 17개 |
| 2026-08-20 | Attribute 실사용 가능 범위 | 34개 중 실제로 값을 채울 수 있는 키가 몇 개인지 | 원천 확인 3개(PARKING_CONVENIENCE·BUSINESS_HOURS·KIDS_FRIENDLY) 중 둘은 REDESIGN·MOVE_TO_PURPOSE 대상 | **채울 수 있는 AttributeKey는 PARKING_CONVENIENCE 1개.** L0 §5 "목적 필터 원천 없음" 제약의 구체적 크기가 확정됨 |
| 2026-08-20 | NO_SOURCE 확보 리스크 건수 | 1차 분류의 "실제 확보 대상 28건"이 실측 후에도 맞는지 | TOURIST_SPOT의 PRICE_LEVEL·ENTRANCE_FEE_LEVEL·VISIT_DURATION 3조합이 LOW_COVERAGE → 실측 0% | **28건 → 31건으로 증가.** 명세 추정으로 "값이 적다"였던 것이 실측에서 "필드가 없다"로 바뀜 |
| 2026-08-20 | GO-55 NO_SOURCE 판정 | TourAPI 실측만으로 AttributeKey를 NO_SOURCE로 확정한 것이 타당한지 [조사: 공공데이터포털·산림청·문화포털·KOPIS·부산/울산/경남 포털·관광공사 계열·민간 API 약관. 웹 문서 기준, 응답 필드 미실측] | 산림청 등산로에 **난이도·소요시간**, 부산명소정보에 **이용요금**, 관광지 표준데이터에 **주차가능수·수용인원수** 필드가 명세에 존재 | **판정 범위 정정.** "TourAPI에 없다"를 "원천이 없다"로 확대한 것이 오류. 6개 키의 NO_SOURCE 판정 보류. 4축 분류와 최종 처리 유형은 키의 성격 판정이므로 유지. 실측은 `GO-125` |
| 2026-08-20 | 카페 내부 환경 13개 | 확장 조사에서 NOISE_LEVEL·OUTLET_ACCESSIBILITY 등의 공공·전문·지역 원천이 있는지 | 어느 원천에서도 발견되지 않음. 기관이 조사하지 않는 사실 | **기존 판단 유지.** 다만 제거하지 않고 UNKNOWN으로 둔다. 사용자 입력·직접 수집 여부는 별도 결정 |
| 2026-08-20 | 민간 장소 API 적재 가능성 | 카카오 로컬·네이버 지역검색·Google Places를 정본 적재에 쓸 수 있는지 | Google은 사전 수집·장기 캐시 제한이 명시. 카카오·네이버는 DB 영구 저장 허용 근거를 공개 문서에서 찾지 못함 | **정본 적재 대상 아님.** 대조·탐색용 가능 여부만 별도 판단. 화면에 정보가 있다는 것이 적재 근거가 되지 않음 |
| 2026-08-20 | 원천 부재 판정 기준 | 특정 원천의 실측 0%를 `NO_SOURCE` 근거로 쓴 것이 타당한지 | 오늘 두 차례 같은 오류 — 매칭 키(주차장 1개 원천 → 전체 일반화), NO_SOURCE(TourAPI 1개 원천 → 확보 불가 판정) | **규칙 신설.** L-1 §6에 "부재를 일반화하지 않는다" 추가. 공개데이터·전문기관·지자체·민간·계산추출 **다섯 범위**를 모두 확인·기록해야 `NO_SOURCE` 후보. 그 전은 `SOURCE_SEARCH_PENDING`. L0 §4에 2행 추가 |
| 2026-08-20 | attribute_sources.csv status | 새 기준을 69개 조합에 적용 | 52개가 탐색 미완 상태였음 | **52개를 `SOURCE_SEARCH_PENDING`으로 변경.** 현재 `NO_SOURCE`로 확정된 조합은 **0개** |
| 2026-08-20 | PARKING_LOT 2개 조합 | 새 기준 적용 중 `NO_SOURCE`로 남은 2건 재확인 | 주차장 표준데이터 실측(GO-79)에 **운영시각 100%·구획수 99.9%·요금정보 100%** 존재. TourAPI에 PARKING_LOT 콘텐츠 타입이 없어 미반영이었음 | **`MEASURED_AVAILABLE`로 정정.** 새 기준이 실제로 잘못된 NO_SOURCE를 잡아낸 첫 사례 |
| 2026-08-20 | survey 문서 구조 | 8절 신설이 기존 "PlaceType별 후보 원천 관리"(A/B표, 7단계 상태 어휘)와 병행되는지 확인 | 같은 목적의 표가 두 형식으로 존재하게 됨. 전국관광지표준데이터·KOPIS·문화포털이 양쪽에 중복 기재 | **8절 삭제, A/B표로 통합.** 신규 발견(산림청 등산로·부산명소정보·부산맛집정보·반려동물 동반여행·전국휴양림표준데이터)만 기존 표에 행으로 추가. 울산 지역 포털이 표에 없던 것도 함께 기재 |
| 2026-08-21 | 목적 필터 원천 없음 (L0 §5) | 확정된 제약이 추천 구현에 반영돼 있는지 [측정: `gabojago-backend/src/main/java`의 `AttributeKey` 참조 4개 파일 grep, `attribute_sources.csv` 69조합 대조. Python·프론트·미머지 브랜치 미측정] | `PlaceScoringService.DEFAULT_PREFERRED_ATTRIBUTES` 4개(PHOTO_SPOT·SCENERY_QUALITY·INTERIOR_MOOD·DESSERT_VISUAL)가 전부 `SOURCE_SEARCH_PENDING`. 추가 참조하는 PRICE_LEVEL도 실측 0%. 원천이 확인된 것은 PARKING_CONVENIENCE뿐. INTERIOR_MOOD·DESSERT_VISUAL은 카페 내부 환경 13개에 속해 공공 원천 확보 가망이 없다 | **구현과 확정 제약의 충돌을 기록만 한다. 지금 고치지 않는다(`LATER`).** GO-125의 다섯 범위 탐색이 끝나야 4개의 최종 상태가 정해지므로, 지금 로직을 고치면 근거 없이 두 번 고치게 된다. 점수 모델과 사용자 입력·직접 수집 정책을 다룰 때 함께 처리한다. GO-125 진행에는 영향 없음 |
| 2026-08-22 | GO-125를 연 근거 (2026-08-20 `GO-55 NO_SOURCE 판정` 행) | "명세에 난이도·소요시간 필드가 존재"를 근거로 4개 키의 NO_SOURCE를 보류했는데, 필드 존재가 사용 가능을 뜻하는지 [측정: 산림청 등산로 부울경 448개 산 전량, 구간 13,624건] | 필드는 채움률 99.8~100%로 **존재.** 그러나 소요시간은 산 단위 합계가 방문 소요가 아니라 등산로 총연장이었다(금정산 6,785분 = 113시간). 난이도는 산 단위 최빈값 기준 98.0%가 `쉬움`이라 집계 규칙에 따라 변별력이 사라진다 | **근거의 성격 정정.** 8/20에 "TourAPI에 없다"를 "원천이 없다"로 확대한 것과 **방향만 반대인 같은 오류** — 이번엔 명세상 존재를 사용 가능으로 확대했다. 채움률만으로 원천을 판정할 수 없고 **값의 의미와 집계 단위를 함께 확인**해야 한다. 수치는 `GO-125`, 상태는 `attribute_sources.csv`에 기록 |
| 2026-08-22 | 원천 운영 모델 — 기준선 (L0 §4) | 인허가 기준선을 업종별로 잡을지 지역별로 잡을지 정해진 적이 있는지 [측정: `source-downloads.md` 기록분과 `data/raw/standard/` 실제 파일 대조] | 확정표에 **행이 없다.** 세 축(기준선·변경 감지·종료 신호)을 따로 정의한다는 규칙만 있고 인허가의 기준선 단위는 미정. 실제로는 업종×시도로 21개 파일을 받았는데, LOCALDATA는 **지역별 전체 다운로드**를 제공해 부울경 3개 파일로 같은 내용을 덮을 수 있다 | **결정 아님 — 미확정이 드러난 것을 기록.** 기존 결정이 깨진 게 아니라 정한 적이 없다. `PlaceSubtypeCode` 64개 중 4개 업종만 받은 상태라 업종을 늘릴수록 파일 수가 선형으로 늘고 변경 감지 비용이 갈린다. 이미 받은 21개는 GO-120에서 실측을 마쳐 재다운로드하지 않는다. **다음에 업종을 추가하는 시점에 기준선 단위를 함께 정한다** |
| 2026-08-22 | 원천 운영 모델 — 기준선 (L0 §4) | 같은 날 열어둔 미확정 항목에 실측 근거를 붙임 — LOCALDATA 지역별 전체 zip이 기존 업종별 21개 파일을 덮는지 [측정: 5업종 × 4지역 20쌍을 `관리번호` 집합으로 전량 대조] | **기존에만 있고 신규에 없는 `관리번호` 0건** (20쌍 전부). 신규에만 있는 것은 일반음식점 +2~18, 휴게음식점 +4~9. 제과점·숙박업·관광숙박업은 12쌍 중 12쌍 완전 동일. 컬럼 구성도 동일 | **지역별 파일이 업종별의 상위집합임이 확인됨. 기준선 단위는 여전히 미확정.** 덮는다는 사실과 기준선을 지역별로 정하는 판단은 다르다. 변경 감지 비용을 재기 전에는 정하지 않는다. 업종별 21개 파일은 삭제하지 않는다 — 8/20과 8/22 두 스냅샷이 변경 감지 실측의 재료다 |
| 2026-08-22 | `PlaceSubtypeCode` 모집단 공백 | 64개 subtype 중 모집단 원천이 없던 것에 LOCALDATA 지역별 zip이 대응하는지 [측정: 195개 업종 × 부울경 3지역 전량, 영업중 기준. 대응 subtype은 업종명 기준 매핑 후보이지 확정 아님] | 16개 업종이 대응. 유흥주점 7,376 · 농어촌민박 4,823 · 단란주점 2,779 · 체육도장 2,405 · 체력단련장 2,321 · 도시민박 1,314 · 골프연습장 1,300 · 야영장 522 · 테마파크 487 · 공연장 179 · 박물관미술관 146 · 수영장 122 · 한옥체험 119 · 골프장 53 · 승마장 21 | **survey A표에 실측값 반영.** `SPORTS_FACILITY`·`GOLF`·`HORSE_RIDING`·`PERFORMANCE_HALL`·`MUSEUM`처럼 모집단이 아예 없던 subtype에 원천이 생겼다. 중복률 측정 전이므로 기준선에는 넣지 않는다 |
| 2026-08-22 | ACCOMMODATION base (2026-08-20 확정) | 농어촌민박업이 기존 base를 깨는지 [측정: 부울경 영업중 4,823건 전량 × base 6,385건 전량, 지번·도로명·도로명+상호 3방식] | 중복 지번 0.1% · 도로명 0.1% · 도로명+상호 0.0%. **사실상 전량 순증**이며 base를 +75% 늘린다. 농어촌정비법 별개 인허가라 원래 겹치지 않는다. 폐업일자 채움 100%로 명시적 종료 상태 제공. 객실수 98.5%(중앙 3) | **기존 결정 유지 — base를 바꾸는 것이 아니라 더하는 것이다.** 숙박업·관광숙박업이 틀린 것이 아니라 농어촌민박이 그 범위 밖이었다. **핵심 기준인 최신성 미검증이라 채택하지 않고 대기 후보로 기록.** 스냅샷이 1개뿐이라 변경 감지율은 재수집 전에는 잴 수 없다 |
| 2026-08-22 | 매칭 키 편차 (L0 §5) | "지번을 기본 키로 쓴다"가 새 원천에서도 유지되는지 [측정: 농어촌민박업 부울경 영업중 4,823건 전량. 다른 신규 업종 15종은 미측정] | 지번 **84.4%** — 지금까지 잰 인허가 원천(98.6~100%) 중 유일하게 낮다. 결측 751건 중 65%가 2024~2026년 인허가이고(전체는 2019년 최다) 결측 건의 도로명은 100%. **무작위 누락이 아니라 최근 등록분일수록 도로명만 입력하는 체계적 패턴** | **적용 범위에 예외가 관측됨. 방침은 아직 바꾸지 않는다.** 원천 1개의 관측이므로 다른 신규 업종에서도 같은 패턴인지 확인한 뒤 판단한다. 확인 전에는 이 원천에 한해 도로명을 1차 키로 쓰는 것을 검토한다 |
| 2026-08-22 | 필드 존재 ≠ 사용 가능 (같은 날 산림청 행) | 같은 오류가 다른 원천에서도 나는지 [측정: 농어촌민박업 40컬럼 전수 채움률] | `조식제공여부`·`건물형태구분명`·`화장실종류명` 3개가 **채움률 0.0%**. 셋 다 숙소 추천에 바로 맞는 이름이다. `전화번호`도 3.6% | **같은 유형이 반복됨을 확인.** 헤더만 보고 필드 목록을 문서에 옮기면 없는 것을 있다고 적게 된다. 원천 실측에는 컬럼 존재가 아니라 **채움률**을 반드시 포함한다 |
| 2026-08-22 | 인허가 증분 기준 적합성 미확정 (L0 §5, `GO-123`) | 8/20·8/22 두 스냅샷으로 `데이터갱신시점` 증분 커서가 실사용 가능한지 [측정: 부울경 일반음식점 324,944 · 휴게음식점 96,597 · 숙박업 10,793 전량, 2일 간격. 수집 메타데이터 3개는 비교에서 제외. **2일 창 1회이며 일괄 갱신 시기는 포함되지 않음**] | **사라진 `관리번호` 0건.** 의미 필드가 바뀐 행에서 커서도 갱신된 비율 **100%**(80/80, 17/17, 6/6) — 변경을 놓치지 않는다. 반대로 커서가 이동한 행 중 실제 변경은 **정밀도 68~86%**(헛걸음 14~32%). 2일간 커서 이동은 324,944건 중 105건(0.03%). 영업상태 전환 44건 실관측 | **증분 커서는 후보를 좁히는 용도로 유효함이 실측됨.** 재현율 100%라 변경을 빠뜨리지 않고, 헛걸음은 L0 §4가 이미 요구하는 의미 필드 비교로 걸러진다. `최종수정시점`도 동일 수치라 둘 중 무엇을 써도 같다. **단 2일 창 1회 측정이며 47% 일괄 갱신이 일어난 시기는 재지 않았다.** 그 구간의 헛걸음 비용은 미측정이므로 `GO-123`을 닫지 않는다 |
| 2026-08-22 | 농어촌민박업 최신성 | 채택 가부를 정하기 위해 최신성을 재려 했으나 측정 가능 범위를 먼저 확인 [측정: 부울경 7,574건 전량의 인허가일자·폐업일자·데이터갱신시점 최신값. **스냅샷 1개**] | 반영 지연은 인허가 4일 · 폐업 10일 · 데이터갱신시점 2일로 대조군 일반음식점(3·3·2일)과 **동등**. 그러나 **변경 감지율은 스냅샷이 1개뿐이라 잴 수 없다** | **채택 보류 — 최신성의 두 축 중 하나만 측정됐다.** 반영 지연은 확인됐고 변경 추적은 미측정이다. 같은 LOCALDATA 계열 3개 업종에서 커서가 작동했지만 **그것은 구조적 유사성이지 이 원천의 실측이 아니다.** 계열 일반화는 이 프로젝트가 반복해 잡아온 오류 유형이므로 적용하지 않는다. 재다운로드 후 판정한다 |
| 2026-08-22 | BAR subtype 확장 (survey 3절 "중복률·신규 비율 먼저 측정") | 유흥주점·단란주점이 현재 BAR base와 겹치는지 [측정: 부울경 영업중 유흥 7,376 · 단란 2,779 × base 13,224 전량. 상호는 법인격·괄호·특수문자 제거 정규화, 주소는 필드별 정규화. **폐업 레코드와 좌표 매칭은 미측정**] | 지번+상호 완전 일치는 유흥 **2건** · 단란 **0건**. 주소만 겹치는 448건(5.0%)은 표본 확인 결과 전부 같은 건물의 다른 업소였다. 유흥↔단란 사이 도로명+상호 일치도 26건뿐 | **10,085건이 순증으로 확인됨.** 중복은 병합 문제가 아니다. 그러나 **기준선에 넣지 않는다** — 유흥주점 업태의 44.9%(룸살롱 2,973·카바레 300·고고클럽 41)가 서비스 노출 적합성 판단 대상이고, 이는 측정이 아니라 제품 결정이다. 단란주점 2,779건은 노출 쟁점이 상대적으로 작다 |
| 2026-08-22 | 다중 원천 병합 · 매칭 키 (L0 §4, §5) | 주소 정규화에서 괄호를 일괄 제거해도 되는지 [측정: 유흥주점 7,352 · 일반음식점 106,003 · 농어촌민박 4,072의 지번·도로명 괄호 내용 전수] | **필드마다 의미가 다르다.** 지번주소 괄호는 **층 정보**(유흥 15.5% · 일반음식점 6.0%가 `(2층)`·`(지하1층)`), 도로명주소 괄호는 **법정동**(80~89%). 괄호를 일괄 제거하니 같은 건물 다른 층 업소가 중복으로 잡혀 유흥주점 겹침이 4.8%→6.4%로 부풀었다 | **정규화 규칙 분리 필요.** 도로명은 괄호 제거, 지번은 층 정보 보존이 맞다. 2026-08-20에 측정한 "자동 병합 가능 74.4% / 한 주소 2건 이상 19.0%"는 이 구분 없이 계산했을 가능성이 있어 **P5 지번 정규화 작업에서 재계산 대상**이다. 첫 계산은 내가 괄호를 지워 겹침을 부풀린 것을 표본 확인으로 잡았다 |
| 2026-08-22 | 분류 3축 (L0 §2-2) | `PlaceSubtypeCode` 64개·`AttributeKey` 34개가 원천이 구분하는 것을 담아내는지 [측정: LOCALDATA 지역별 전체 195개 업종의 업종·업태 구분과 enum 값 대조. 부울경 영업중 기준. TourAPI·상가정보의 분류 체계는 미대조] | 원천이 나누는데 축이 못 나누는 사례 다수. 유흥주점 **13개 업태**(룸살롱 2,973~극장식당 2)가 `GENERAL_BAR`·`TRADITIONAL_PUB` 2개에, 체육도장·체력단련장·수영장·종합체육시설 4종이 `SPORTS_FACILITY` 1개에, 골프장과 골프연습장이 `GOLF` 1개에 뭉친다. `AttributeKey`도 근접 주차장 존재율·객실수를 담을 키가 없다 | **축을 지금 늘리지 않는다. 대신 확장 조건을 명문화했다.** L0 §4에 `분류 축의 확장 조건`·`원천 저장 범위` 2행 추가 — 현재 개수는 상한이 아니고, 확장의 전제는 새 enum 값을 미리 만드는 것이 아니라 **원천 원본을 staging에 보존하는 것**이다. `업태구분명`처럼 `InfoKey` 매핑이 없는 컬럼도 지우지 않는다. 확장은 실제 추천 질의가 요구할 때(`L-1` §3), 보존은 지금. 구체적 대기 목록은 survey 3절이 소유 |
| 2026-08-22 | 문서 커버리지 | "원천 응답 값 → subtype·Attribute 분류로 확장하는 경로"가 문서에 있는지 [측정: `data-import/docs/` 4개 문서 · `mappings/` 8개 파일 · L0 전수 확인] | **부분만 있었다.** 있는 것 — L0 §2-1 데이터 4층(개념), `info-attribute-split.md`(Info/Attribute 경계), survey §4(`InfoKey` 후보), `mappings/tour_api_subtypes.csv`(239행, **TourAPI 전용**). **없는 것** — 인허가·상가정보의 subtype 매핑 파일, 원천 값 어휘 기록, 값→허용 어휘 변환 규칙, 확장 판정 절차. `RESTAURANT`·`CAFE`·`BAR`·`ACCOMMODATION` base가 전부 인허가인데 그 안에서 subtype을 가르는 규칙이 문서에도 파일에도 없었다 | **`classification-mapping.md` 신설.** 규칙·절차는 이 문서가, 값은 `mappings/*.csv`가 소유한다. 실측된 값 어휘(업태구분명 28·20·13·7종, 영업상태명 3종)를 함께 기록. 배정 함정 4가지를 실측 근거로 명시 — 특히 **휴게음식점의 `편의점` 3,945건은 `SHOP`의 `CONVENIENCE_STORE`**라 파일 단위로 PlaceType을 정하면 틀린다. 배정 단위는 파일이 아니라 업태 값이다. AGENTS.md 읽기 순서 4번에 연결 |
| 2026-08-22 | 인허가 subtype 매핑 부재 (classification-mapping 2절) | `RESTAURANT`·`CAFE`·`BAR`·`ACCOMMODATION` base가 전부 인허가인데 subtype 배정 규칙이 없던 문제 해소 [측정: 부울경 영업중 27개 업종 파일 169,086건 전량. 195개 업종 중 `PlaceSubtypeCode` 대응 후보가 있는 27개만 대상] | `mappings/license_subtypes.csv` 98행 작성. **`CONFIRMED` 40행(111,207건·65.8%) · `NEEDS_REVIEW` 37행(51,880건·30.7%) · `UNMAPPED` 21행(5,999건·3.5%)**. 누락 검증에서 원본 98조합·169,086건과 완전 일치 | **애매한 것은 추측하지 않고 target을 비웠다.** `tour_api_subtypes.csv`의 2종 어휘에 `UNMAPPED`를 추가 — 고정 장소가 아니거나(푸드트럭·출장조리) 대응 subtype이 없거나(룸살롱) 노출 정책이 미결인 경우다. 휴게음식점 `백화점`·`고속도로`·`철도역구내`·`공항`은 업태가 아니라 **입지**여서 subtype을 결정하지 못한다 |
| 2026-08-22 | 매핑 검증 방법 | 사업장명 키워드 적중률로 오분류를 판정할 수 있는지 [측정: 부울경 영업중, 대상 업태 4종 + 대조군 4종] | **적중률 단독으로는 판정 불가.** 오적중 기준선이 한식 0.8% · 중국식 0.2%인데 휴게음식점 `커피숍`은 56.2%(신뢰 가능), 일반음식점 `까페`는 15.1%(부분만 맞음)로 갈렸다. 반대로 유흥주점 `비어(바)살롱`의 노래 키워드 52.6%를 오분류 신호로 읽었으나, 음성 대조로 둔 `룸살롱`도 **44.5%**였다 | **대조군 없는 적중률을 근거로 쓰지 않는다.** 유흥주점은 13개 업태 전반이 노래주점 형태라 그 키워드에 변별력이 없다. 대조군을 재지 않았으면 틀린 사유를 csv에 기록할 뻔했다. 5개 행의 `note`를 실측 근거로 정정. 검증 절차를 `classification-mapping.md` 5-1절에 규정 |
| 2026-08-22 | survey A표 PlaceType 오기 | 8/22에 내가 추가한 행의 `PlaceType` 열이 enum과 맞는지 [측정: `PlaceSubtypeCode.java` 64개 전수 대조] | 테마파크·공연장 2행을 `ACTIVITY`로 적었으나 `THEME_PARK`·`PERFORMANCE_HALL`은 **`TOURIST_SPOT` 소속**이다 | **2행 정정.** 매핑 파일을 만들며 enum을 전수 확인하다 발견했다. 문서에 PlaceType을 적을 때는 enum에서 확인한다 |
| 2026-08-22 | `license_subtypes.csv` NEEDS_REVIEW 상위 3업태 | 업태 이름으로 내용을 알 수 없는 3개를 상호로 가를 수 있는지 [측정: 부울경 영업중 **모집단 전량** — 일반음식점 `기타` 14,047 · 휴게음식점 `기타 휴게음식점` 5,357 · 일반음식점 `경양식` 5,269. 상호 키워드 + 프랜차이즈 브랜드 26종. 표본이 아니라 전량이라 표본오차 없음] | **정확도는 높고 재현율이 낮다.** 분류된 건은 버킷별 육안 검수 12/12 일치. 그러나 미분류가 각각 62.0% · 46.3% · 64.3%로 남고, 브랜드를 추가해도 1.5~3.3%p만 회수됐다. 남은 미분류는 상호에 업종 정보가 없는 고유명사다 | **일부만 분리 가능, 나머지는 미확정 유지.** 분리 가능 — 휴게음식점의 편의점 434(SHOP) · PC방 460 · 카페 1,140, 일반음식점 `기타`의 카페 1,891. 미확정 유지 — 미분류 잔여 14,580건. **`기타`와 `경양식`의 `target`을 비웠다** — `기타`는 카페 13.5%가 섞여 PlaceType조차 갈리고, `경양식`은 양식 지지가 21.7%뿐이라 나머지 78%를 WESTERN으로 적재할 근거가 없다. 근거 없이 채워둔 것을 되돌린 것이다 |
| 2026-08-22 | 상호 기반 분리의 위치 | 상호에서 추론한 분류를 `license_subtypes.csv`에 넣어도 되는지 | 이 파일의 키는 `(업종, 업태구분명)`이고 값은 **원천이 준 분류**다. 상호 추론은 원천 사실이 아니라 우리의 해석이라 층이 다르다 | **넣지 않았다. 측정만 기록.** 규칙 층을 새로 두는 것은 설계 결정이므로 근거만 남기고 판단은 미룬다. Info와 Attribute를 가르는 것과 같은 경계다 |
| 2026-08-22 | PC방 subtype 부재 | 휴게음식점에서 상호로 갈린 PC방 460건이 갈 곳이 있는지 [측정: `PlaceSubtypeCode` 64개 전수 대조, 부울경 인허가 업종 목록] | **대응 subtype 없음.** 전용 원천 `문화_인터넷컴퓨터게임시설제공업`이 부울경 영업중 **2,454건** 따로 있다. 휴게음식점의 460건은 음식 판매를 겸해 양쪽 등록된 것일 수 있으나 **중복 미측정** | **축 확장 후보로만 기록.** survey 3절 확장 후보의 성격과 같다. 서비스 노출 여부는 제품 결정이라 판단하지 않는다. 460건을 지금 어디에도 배정하지 않는다 |
| 2026-08-23 | L0 §6 실행 단계 JQL | 문서만 읽고 다음 작업을 찾을 수 있는지 [측정: L0 §6 · `attribute-coverage.md` 남은 작업 · `decision-log.md` 대조] | JQL이 `fixVersion = "P1 TourAPI 상세 실측"`으로 고정돼 있는데 실제 진행 중인 `GO-125`는 TourAPI 상세가 아니라 다섯 범위 원천 탐색이다. 저장소만 읽으면 잘못된 필터에서 다음 작업을 찾는다 | **stale 값 제거.** 버전명을 새로 박지 않고 `statusCategory != Done ORDER BY Rank ASC`로 바꿨다. 현재 최우선이 `GO-125`라는 것과 그 범위의 소유 문서(`attribute-coverage.md` 남은 작업)만 가리키고 항목은 옮겨 적지 않는다 — §6의 "문서로 이중 관리하지 않는다"를 유지하기 위해서다 |
| 2026-08-23 | 산림청 4개 조합의 `status` 표기 | `SOURCE_SEARCH_PENDING` 하나로 "한 원천을 재보고 탈락시킴"과 "아직 아무 원천도 안 봄"이 구분되는지 [측정: `attribute_sources.csv` 69행 · `attribute-coverage.md` 대조] | 두 상태가 같은 값으로 저장돼 `status`만 보면 남은 작업이 얼마인지 갈리지 않는다. 구분은 `measured_note`에만 있었고 4행 중 1행(`PHYSICAL_INTENSITY`)에는 그 문구조차 없었다 | **새 status 값을 만들지 않고 표기만 통일했다.** 어휘를 늘리는 것은 설계 결정이라 근거 없이 하지 않는다. 4행의 `measured_note`에 "이 원천의 부적합은 다른 원천 탐색을 끝냈다는 뜻이 아니라 계속 SOURCE_SEARCH_PENDING이다"를 동일 문구로 넣고, `attribute-coverage.md`에 두 상태의 차이를 명시했다 |
| 2026-08-23 | GO-125를 연 근거 3원천 중 2번째 — 전국관광지정보표준데이터 (2026-08-20 `GO-55 NO_SOURCE 판정` 행) | "관광지 표준데이터에 주차가능수·수용인원수 필드가 명세에 존재"가 실제로 쓸 수 있는지 [측정: 부울경 208건 전량, 2026-08-23. 연결은 TourAPI 12+14+28 부울경 2,701건 대비 이름·좌표만. **최신성·API 경로 미측정**] | `주차가능수`는 **숫자**로 값>0 87.0%(181/208), 고유값 89개로 분포가 넓어 텍스트 파싱 없이 등급화 가능 — 쓸 수 있다. `수용인원수`는 값>0 97.6%지만 **단위가 원천 안에서 혼재**(을숙도 4,500,000 = 연간 방문객 vs 창녕 만년교 100 = 동시 수용)라 `CROWD_LEVEL`에 부적합. 이름 정확 일치 54.3%(산림청 8.3%와 대비)로 연결은 작동하나, TourAPI 관광지(12) 1,891건 대비 **실효 연결 3.6%**(68건) | **보조 원천 후보. 모집단 대체 탈락, 채택 아님.** `PARKING_CONVENIENCE`는 `FREE_TEXT` 유지 — 숫자가 더 좋은 재료여도 3.6%를 위해 우선 원천을 바꾸지 않는다. `CROWD_LEVEL`은 `SOURCE_SEARCH_PENDING` 유지. **`수용인원수`는 산림청 소요시간과 같은 유형의 실패다** — 채움률 97.6%가 아니라 값의 단위를 봐야 걸러진다. 세 번째 원천(부산명소정보)은 미측정으로 남는다 |
| 2026-08-23 | 부울경 건수의 세는 기준 (`source-downloads.md`) | 기록된 189행이 재현되는지 [측정: 전국관광지정보표준데이터 852행 전량] | **기준에 따라 갈린다** — 지번 172 · 도로명 189 · 합집합 208. 기록된 189는 도로명 기준이었으나 문서에 기준이 적혀 있지 않았다 | **기록에 기준을 붙였다.** 지번 채움률이 82.7%로 낮은 원천이라(농어촌민박 84.4%와 같은 패턴) 어느 필드로 세느냐가 17% 차이를 만든다. 건수를 적을 때는 세는 필드를 함께 적는다 |
| 2026-08-23 | 같은 원천 안의 자유텍스트 vs 숫자 | 두 필드가 같은 사실을 말하는지 [측정: 전국관광지정보표준데이터 부울경 208건 전량] | `공공편익시설정보`에 `주차장` 토큰이 있는 것은 93건(44.7%)인데 `주차가능수`>0은 181건(87.0%)이다. 자유텍스트 쪽이 누락이다 | **결과만 기록. 숫자 필드를 쓴다.** 자유텍스트 시설 목록의 부재를 시설 부재로 읽으면 안 된다는 사례다. 한 원천 안에서도 필드별로 채움 품질이 다르다 |
| 2026-08-23 | `GO-125`의 작업 목적 소유 문서 | 탐색 원칙이 대화에만 있고 저장소에서 복원되지 않던 문제 [측정: `AGENTS.md` 읽기 순서 · `data-import/docs/` 4개 문서 · L0 §6 대조] | `attribute-coverage.md`가 "1번이 이 감사의 핵심 질문"으로 **52칸 채우기를 목적처럼** 서술하고 있었다. `AttributeKey` 34개는 확정이 아닌데(L0 §2-2) 그 빈칸을 탐색 우선순위 근거로 쓰면, 키가 바뀔 때 탐색이 통째로 헛일이 된다 | **`other-source-survey.md` 0절 신설 — 이 절이 목적을 단독 소유한다.** 목적은 빈칸 채우기가 아니라 **staging·`Info` 후보로 보존할 원천 사실 확보**이고, 재는 것은 원본 필드·채움률·값 분포·값의 단위·연결성·모집단 범위·순증 가치 7가지다. `AttributeKey` 매핑·신규 키·파생 규칙은 후속 단계로 명시했다. A/B표는 후보 목록이지 요구사항이 아니다. `attribute-coverage.md`는 커버리지 현재 상태만 소유하고 목적은 0절로 링크한다 — 같은 내용을 두 곳에 쓰지 않는다. `AGENTS.md` 읽기 순서 4번에 "원천을 새로 보기 전에 0절을 반드시 읽는다"를 넣었다 |
| 2026-08-23 | `other-source-survey.md` 8-5 "범위 5 후보 소진" vs A표 `원본 확보·중복 미측정` | 두 서술이 동시에 성립하는지 [측정: `인허가_지역별` 부울경 3개 시도 195개 업종 파일 전수 존재 확인 + 7개 후보군 건수 재현] | **둘 다 맞다.** 8-5는 *범위 5(보유 원본에서 새 후보 발굴)의 탐색* 소진을 말하고, A표 7종은 *범위 1(포털 검색)에서 먼저 발굴돼 A표에 행이 있던* 후보라 실측이 남아 있었다. 7종 전부 로컬 원본이 존재하고 문서 기록 건수가 그대로 재현됐다(관광펜션 209 · 외국인민박 1,314 · 한옥체험 119 · 야영장 522 · 테마파크 487 · 공연장 179 · 체육시설 6,222). 다만 8-5의 열거 합계는 3,144인데 3,148로 적혀 있다 | **stale 아님 — 문장의 범위 한정이 약한 것이다.** "현재 가진 원본으로 잴 수 있는 범위 5 후보는 소진됐다"가 *로컬로 잴 것이 없다*로 읽힌다. 8-6을 신설해 이 구분을 적고 숙박 계열 3종부터 실측했다. 합계 4건 차이는 미해결로 남긴다 |
| 2026-08-23 | ACCOMMODATION base (2026-08-20 확정) | 숙박 계열 3업종이 base를 깨는지 [측정: 부울경 영업중 외국인민박 1,314 · 관광펜션 209 · 한옥체험 119 전량 × base 6,385 ∪ 농어촌민박 4,823. 도로명+상호 동시 일치. **변경 감지율·좌표 변환 정확성 미측정**] | 외국인민박 겹침 0.0%(전량 순증) · 한옥체험 2.5% · **관광펜션 59.3%**. 3업종 모두 지번 100%, 좌표 EPSG:5174, `(개방자치단체코드, 관리번호)` 조합키로만 1:1 | **기존 결정 유지 — base를 바꾸지 않는다.** 외국인민박·한옥체험은 농어촌민박과 같은 성격의 *더하는* 후보이나 **최신성 미검증이라 채택 아님**. 관광펜션은 확장 가치가 작아 대기 후보에서도 순위가 낮다 |
| 2026-08-23 | 다중 원천 병합 (L0 4절) | 인허가 업종 간 주소 겹침이 항상 "같은 건물 다른 업소"인지 [측정: 관광펜션 209건의 base·농어촌민박 겹침 124건, 표본 11건 육안 대조] | **아니다.** 관광펜션 겹침은 상호까지 같은 **같은 사업장의 이중 인허가**였다(민박 2024-01-18 → 펜션 2025-03-25 형태, base 쪽 업태는 숙박업(생활)·여관업). 반면 외국인민박의 지번 겹침 13건은 표본 8건 전부 상호가 달라 기존 패턴대로 같은 건물 다른 업소였다 | **겹침의 성격을 원천 쌍마다 따로 판정한다.** 이중 인허가 쌍은 병합 대상이지 순증이 아니다. 또한 **대조군에 대기 중인 확장 후보(농어촌민박)를 빠뜨리면 순증이 과대계상된다** — 관광펜션 순증은 base만 보면 87.6%, 농어촌민박까지 넣으면 40.7% |
| 2026-08-23 | 필드 존재 ≠ 사용 가능 (2026-08-22 산림청·농어촌민박 행) | 같은 오류가 숙박 계열 3업종에서도 나는지 [측정: 문화 계열 48컬럼 전수 채움률, 부울경 영업중] | **재현됐다.** `선박제원`·`선박척수`·`선박총톤수`·`무대면적`·`기념품종류`·`좌석수`가 3업종 모두 0%(문화 계열 공통 스키마 잔재). **관광펜션은 `객실수`가 209건 전량 빈값**인데 같은 컬럼이 농어촌민박 98.5% · 외국인민박 65.9%다. `시설규모`는 `시설면적`의 정수 반올림으로 중복 필드 | **컬럼 채움률은 업종 파일마다 따로 잰다.** 계열이 같아도 상속하지 않는다. 보존 시 `시설규모`/`시설면적`은 한쪽만으로 충분하다 |
| 2026-08-23 | 매칭 키 편차 (L0 5절) | 농어촌민박의 지번 84.4% 예외가 다른 문화 계열에서도 나타나는지 [측정: 3업종 부울경 영업중 전량] | **아니다 — 3업종 모두 지번 100%다.** 대신 한옥체험은 도로명이 89.9%로 지번보다 낮아 방향이 반대다 | **"지번을 기본 키로" 방침은 이 범위에서 유지된다.** 농어촌민박의 84.4%는 계열 전체 경향이 아니라 그 업종의 특성이다 |
| 2026-08-23 | 최신성 측정 방법 | 농어촌민박에서 쓴 반영 지연 측정을 소규모 업종에 그대로 쓸 수 있는지 [측정: 3업종의 인허가일자·폐업일자·데이터갱신시점 최신값 대 다운로드일 2026-08-22] | 외국인민박(1,314건)은 인허가 3일 전·갱신 2일 전으로 농어촌민박과 동등하나, **관광펜션(209건)·한옥체험(119건)은 최신 인허가일이 22~30일 전**이다. 모집단이 작아 반영 지연인지 신규 인허가 발생 빈도인지 갈리지 않는다 | **소규모 업종은 단일 스냅샷으로 반영 지연을 판정하지 않는다.** 3업종 모두 변경 감지율은 미측정(스냅샷 1개)이며 **최신성 미확정 상태로 남긴다** |
| 2026-08-23 | 순증 측정의 대조군 범위 (2026-08-23 관광펜션 행) | 대조군을 한 계열로 잡으면 과대계상되는 문제가 야영장에서도 나는지 [측정: 부울경 영업중 야영장 522건 전량 × TourAPI 12(1,891)·28(462) · base 6,385 · 농어촌민박 4,823 · 상가정보 387,313. 인허가·상가정보는 주소+상호, TourAPI는 이름+시도+시군구. **좌표 매칭 미측정**] | **재현됐다.** base만 보면 순증 99.0%, TourAPI 레포츠만 보면 71.8%인데 보유 원천 전체로는 **45.4%**다. 야영장은 숙박 계열과 관광 계열에 걸쳐 있어 한 계열만 대조하면 반드시 부풀려진다 | **순증은 보유 원천 전체를 대조군으로 잡고 계산한다.** 원천이 걸치는 계열을 먼저 확인한 뒤 대조군을 구성한다 |
| 2026-08-23 | 중복 판정 방식 | 주소 매칭 하나로 중복을 판정해도 되는지 [측정: 야영장 522건, 주소+상호 방식과 TourAPI 이름+시군구 방식 각각] | 주소로 204건 · 이름으로 154건이 잡혔는데 **둘 다 잡힌 것은 73건뿐**이고 합집합은 285건이다. 두 방식이 서로 다른 장소를 잡는다 | **한 방식만으로 중복률을 확정하지 않는다.** 원천 쌍마다 쓸 수 있는 키가 다르므로 방식별 결과를 함께 기록한다 |
| 2026-08-23 | 필드 존재 ≠ 사용 가능 · 값의 단위 (2026-08-23 전국관광지 `수용인원수` 행) | 같은 유형의 실패가 야영장에서도 나는지 [측정: 야영장 영업중 522건 48컬럼 전수] | **재현됐다.** `시설면적` 채움률 77.4%인데 국립남해편백자연휴양림 3,654,348㎡는 휴양림 전체 부지이고 노랑카라반 0㎡·별바람야영장 17.5㎡는 시설 하나다. 단위(㎡)는 같은데 **측정 대상이 혼재**한다. `지상층수`에 140층, `객실수`는 522건 전량 빈값 | **채움률·단위가 같아도 측정 대상을 확인해야 한다.** 이 필드들은 그대로 등급화할 수 없다. 판단은 P2이며 여기서는 기록만 한다 |
| 2026-08-23 | 이름 매칭의 작동 여부 (2026-08-22 산림청 등산로 8.3% 행) | 이름+시군구 매칭이 원천 쌍에 따라 갈리는지 [측정: 야영장 522 × TourAPI 레포츠 462, 표본 6건 육안 대조] | **작동한다 — 28.2%(147건)이고 표본 6건 전부 같은 장소였다.** 괄호 유지본 145건과 제거본 147건 차이도 2건뿐이다 | **이름 매칭의 실패는 산림청 등산로라는 원천 쌍의 성질이지 방법의 한계가 아니다.** 원천 쌍마다 따로 판정한다 |
| 2026-08-23 | 매칭 키 편차 (L0 5절) | 도로명 결측 패턴이 원천마다 같은 방향인지 [측정: 야영장 영업중 522건의 도로명 결측 64건 전수] | **방향이 반대다.** 농어촌민박은 최근 등록분일수록 도로명만 입력했는데, 야영장은 **오래된 등록분(2014~2017 39건)일수록 도로명이 없다**. 경남 54건 집중. 지번은 100% | **"지번을 기본 키로"가 이 범위에서도 유지된다.** 결측 패턴은 원천별로 다르므로 한 원천의 패턴을 계열로 일반화하지 않는다 |
| 2026-08-23 | TourAPI 부울경 집계 (8-2 규칙 2) | 주소 기준 집계가 재현되는지 [측정: `data/raw/tourism/tour-api/` 12·28 전량 재집계] | 관광지(12) **1,891건으로 기존 기록과 일치**. 레포츠(28)는 462건이며 주차장 근접 계산의 467건은 좌표 유효 기준이라 5건 차이다 | **규칙 유지.** 집계 시 `경상남도` 접두 비교를 고정 길이로 자르면 경남이 통째로 빠진다(이번 측정에서 실제로 발생, 재실행으로 정정). 시도 접두는 문자열 시작 비교로 한다 |
| 2026-08-23 | `other-source-survey.md` 8-3 정정 — 15124679 = LOCALDATA 테마파크업(기타) | 두 원천이 실제로 같은지 [측정: 로컬 LOCALDATA 테마파크업(기타) 4개 시도 1,801행·40컬럼·좌표계. **15124679 원본 미보유로 행 단위 대조 불가**] | **확증도 반증도 못 했다.** 확인된 것은 우리 파일이 LOCALDATA 인허가 스키마(개방자치단체코드·관리번호·상세영업상태코드, 좌표 EPSG:5174)이고 표준데이터 계열(WGS84)과 좌표계가 다르다는 것, 4개 시도 1,801행이 전국 5,733행과 모순되지 않는다는 것뿐이다 | **8-3의 "별개 후보가 아니다"는 보류한다.** A표의 두 행을 합치지 않고, 15124679 원본 CSV를 받아 행·식별자 단위로 대조한 뒤 판정한다 |
| 2026-08-23 | LOCALDATA 지역별 zip의 관할 범위 | 지역별 파일이 그 지역 행만 담는지 [측정: 이미 실측한 16개 인허가 업종 파일의 영업중 행 전수, 파일 지역 대 주소 지역 대조] | **깨지는 사례가 1건 있다.** 경남 `문화_일반테마파크업`의 `(주)다림시스템`이 경기도 광명시 주소·좌표다. 나머지 15개 업종 파일에는 불일치가 없다. 테마파크 부울경은 파일 기준 487 · 주소 기준 486 | **파일 기준과 주소 기준 집계가 다를 수 있음을 기록한다.** 8-2 규칙 2(TourAPI는 주소로 센다)가 LOCALDATA에도 해당한다. 규모가 1건이라 기존 측정치는 정정하지 않고 세는 기준을 함께 적는다 |
| 2026-08-23 | 필드 오용 — 새 실패 유형 | `놀이기구수내역`이 자유텍스트로라도 쓸 수 있는지 [측정: 테마파크 영업중 487건 전수] | 채움률 12.9%인데 `2종 8대` 같은 정상값과 **공문번호(`문화관광체육과-15100 (2020.12.15.)`)·담당자 이메일·전화번호**가 같은 컬럼에 섞여 있다 | **단위 혼재·측정 대상 혼재와 다른 세 번째 유형(입력 칸 오용)으로 기록한다.** 자유텍스트 필드도 값을 열어보기 전에는 보존 가치를 판단하지 않는다 |
| 2026-08-23 | 상가정보 대분류 값 표기 | 대분류로 필터링할 때 표기가 문서와 같은지 [측정: 상가정보 부울경 387,313건 대분류 값 전수] | 실제 값은 **`예술·스포츠`·`수리·개인`·`과학·기술`·`시설관리·임대`**로 가운뎃점이 들어간다. `예술스포츠`로 필터하면 0건이 나온다(이번 측정에서 실제로 발생, 재실행으로 정정) | **대분류 필터는 실제 값 목록을 먼저 확인하고 쓴다.** 0건 결과는 원천에 없다는 뜻이 아니라 필터가 틀렸다는 신호일 수 있다 |
| 2026-08-23 | 레코드 단위 (2026-08-22 산림청 등산로 "산이 아니라 구간" 행) | 같은 유형의 단위 불일치가 공연장에서도 나는지 [측정: 부울경 영업중 179건 전량의 주소별 집계] | **재현됐다.** 179행 중 53행(29.6%)이 26개 주소에 몰려 있고 같은 건물의 대극장·소극장·1관·2관이 별도 행이다. 주소를 장소로 보면 **179행 = 152장소**. 상호 괄호 14건(7.8%)은 홀 이름이라 §8-2 규칙 3의 TourAPI 괄호(지역 표기)와 의미가 다르다 | **건수를 장소 수로 읽지 않는다.** 적재 단위 결정은 P2·P3이므로 여기서는 사실만 기록한다 |
| 2026-08-23 | 원천이 주는 사실의 범위 | 공연장 원천이 규모 정보를 주는지 [측정: 부울경 영업중 179건 33컬럼 전수] | **주지 않는다.** `좌석수`·`객석수` 컬럼 자체가 없고 `시설면적`·`시설규모`는 179건 전량 빈값이다. 이 원천이 주는 것은 존재·위치·영업상태·인허가일이다. 전화번호는 65.4%로 이번에 잰 업종 중 가장 높다 | **업종명이 기대를 만들지만 컬럼 목록이 먼저다.** 공연장 규모가 필요하면 KOPIS 등 다른 원천을 봐야 하며, KOPIS 대조는 로컬 원본이 없어 미측정으로 남는다 |
| 2026-08-23 | survey 1절 A표 `체육시설업 묶음` 업종 목록 | 표에 적힌 6업종이 실제 파일과 맞는지 [측정: `인허가_지역별` 부울경 3개 시도의 `생활_*` 체육 관련 파일 전수] | **2업종이 빠져 있었다.** `생활_종합체육시설업` 44건과 `생활_등록체육시설업` 1건(자동차경주장, 인허가 2007-12-21). 실제는 **8업종 6,267건**이며 A표는 6업종 6,222건으로 적혀 있다 | **A표 정정 필요 — 해당 줄에 미커밋 변경이 있어 8-10에 반영 대기로 남긴다.** 묶음 건수를 인용할 때 업종 목록을 함께 확인한다 |
| 2026-08-23 | 묶음 평균의 위험 (2026-08-22 `ACTIVITY` 레저 10종 행) | 체육시설 묶음에서도 한 업종이 평균을 삼키는지 [측정: 8업종 부울경 영업중 6,267건 전량, 업종별 분리] | **이번에는 삼키지 않는다.** 업종별 순증률이 57.1~100% 범위이고 묶음 평균은 81.6%다. 당구장이 96.6%를 차지했던 레저 10종과 구조가 다르다 | **그래도 묶음 대표값으로 인용하지 않는다.** 승마장업 57.1%와 골프장 90.6%는 다른 결론을 낳는다. 묶음은 분리해서 적는다 |
| 2026-08-23 | 숫자 필드의 이상치 | 체육시설 숫자 필드를 그대로 쓸 수 있는지 [측정: 8업종 영업중 6,267건 전수] | **쓸 수 없다.** `건축물연면적` 체력단련장업 최대 14,166,445㎡(중앙 1,955㎡) · 골프연습장업 5,703,850㎡, `지도자수` 최대 14,750명(중앙 1명), `건축물동수` 최대 303동(중앙 1동) | **야영장(측정 대상 혼재)·테마파크(입력 칸 오용)에 이은 세 번째 유형 — 극단적 이상치로 기록한다.** 이상치 처리 없이 등급화·집계하지 않는다. 판단은 P2다 |
| 2026-08-23 | 종료 신호 판정 (2026-08-23 전통사찰 행) | 비영업 레코드가 0건인 원천이 또 있는지 [측정: 체육시설 8업종의 영업상태 분포 전수] | **골프장(53건)·등록체육시설업(1건)이 비영업 0건**이다. 나머지 6업종은 비영업 대비 폐업일자 채움률 84.6~100%로 명시적 종료 상태를 준다 | **전통사찰과 같은 처리 — 이 파일만으로 종료 신호 유무를 판정하지 않고 재수집 후 판정한다** (`L0` `미관측 ≠ 종료`) |
| 2026-08-23 | A표 `원본 확보 · 중복 미측정` 후보의 실측 | 보유 원본으로 잴 수 있는 A표 후보가 남았는지 [측정: 8-6~8-10에서 7개 후보 9,097건 전량] | **소진됐다.** 외국인민박 1,314 · 야영장 522 · 테마파크 487 · 관광펜션 209 · 공연장 179 · 한옥체험 119 · 체육시설 6,267을 모두 실측했다. **7개 전부 `채택 아님`이고 사유가 같다 — 최신성을 스냅샷 1개로 잴 수 없다** | **채택 판정을 진전시키는 것은 새 측정이 아니라 시간 간격이다.** LOCALDATA 지역별 zip 재수집(7~14일 간격)이 다음 P1 작업 후보이며, 순서는 Jira가 소유한다. survey 9절 신설 |
| 2026-08-23 | 상세 채움률 추정치 (attribute-coverage `미실측 — 추정치`) | 숙소(32)·쇼핑(38)·음식점(39)의 명세 기반 추정치가 실측과 맞는지, 문화시설(14) `usefee`·`spendtime`은 실제로 있는지 [측정: 콘텐츠 타입별 부울경 층화표본 200건 seed 20260820, `detailIntro2` 800회 호출, 수집 실패 0. **좌표 매칭·`overview` 추출 미측정**] | 상세 존재율은 32 **83.0%** · 38 99.0% · 39 **100%** · 14 98.5%로 레포츠(28) 41%와 다르다. 문화시설 `usefee`는 88.3%로 존재하나 **금액이 든 것은 20.1%뿐**이고 `무료` 단독이 51.1%다. `spendtime`은 9.6% | **추정치를 실측값으로 대체한다. 단 `PlaceType별 상태` 표 5행에 미커밋 변경이 있어 반영 대기로 남긴다.** `usefee` 채움률을 요금 확보율로 읽지 않는다 |
| 2026-08-23 | 채움률의 의미 — 0/1 이진 필드 | 채움률 100%가 확보를 뜻하는지 [측정: 숙박(32) 편의시설 12개 필드 · 음식점(39) `kidsfacility`, 상세 존재 166·200건 전수] | **착시다.** 값이 `0`/`1`이라 항상 비어 있지 않다. 값 1의 비율은 beverage 17.5% · barbecue 12.0% · seminar 8.4% · bicycle 0.6%이고 **`beauty`는 166건 전부 0**이다. `kidsfacility`는 1.5%(3건)로 기존 기록 4%와 다르다 | **이진 필드는 채움률이 아니라 값 1의 비율로 기록한다.** 저커버리지 표의 `RESTAURANT.KIDS_FRIENDLY` 4%는 1.5%로 정정 대상이며 해당 줄이 미커밋이라 반영 대기 |
| 2026-08-23 | 자유텍스트 원문 보존의 전제 | 원문을 그대로 보존하면 그대로 쓸 수 있는지 [측정: 4개 타입 상세 필드 전수] | **HTML 마크업이 값에 섞여 있다.** 음식점 `opentimefood` 21.5% · 문화시설 `parkingculture` 14.6% · `usetimeculture` 12.3% · `usefee` 10.3% · `scale` 50.0%에 `<br>` 등이 들어 있다. 숙박 `roomcount`는 `12실`/`12`/`34개` 3종 표기가 섞였고, 문화시설 `spendtime` 19건 중 1건은 운영시간이 들어와 있다 | **보존은 지금 그대로 하되, 원문이 곧 사용 가능은 아님을 기록한다.** 정규화·파싱 규칙은 P2-D/E에서 정한다 |
| 2026-08-23 | TourAPI와 기존 모집단의 연결성 | 상세를 붙일 수 있는지 타입별로 [측정: 상세 존재 건 기준 이름+시도+시군구 정확 일치. 좌표 매칭 미측정] | **타입마다 크게 갈린다.** 음식점(39) 인허가 75.0% · 상가정보 70.5%로 잘 붙는데, **쇼핑(38)은 상가정보 387,313건에 6.6%(13건)**만 붙는다. 숙박(32)은 base 32.5%, 문화시설(14)은 박물관 인허가 18.8% | **`SHOP` base(상가정보)와 TourAPI 쇼핑을 같은 모집단으로 다루지 않는다.** TourAPI 쇼핑은 시장·백화점 층이고 상가정보는 개별 점포다. 숙박 32.5%의 원인(브랜드명 대 사업장명)은 표본 대조로 확인하지 않았다 |
| 2026-08-23 | 원본 식별자 — TourAPI `lcnsno` | 인허가와 ID로 직결되는지 [측정: 음식점(39) 상세 200건 중 `lcnsno` 193건 × 인허가 부울경 영업중 138,453건] | **직결되지 않는다 — `관리번호`와 일치 0/193.** 체계가 다르다(`20050156155` 대 `3250000-101-2015-00041`). 다만 이름+시군구로 붙은 145쌍에서 **`lcnsno` 앞 4자리 = 인허가일자 연도가 131건(90.3%)** 일치한다 | **조인 키로 쓰지 않고 매칭 검증의 보조 신호로만 쓴다.** 불일치 14건이 재인허가·승계인지 오매칭인지는 미확인 |
| 2026-08-23 | TourAPI 상세의 순증 가치 | 인허가·상가정보가 주지 않는 사실이 무엇인지 [측정: 4개 타입 상세 필드 전수, 상세 존재 건 기준] | **음식점 메뉴(`firstmenu` 100% · `treatmenu` 95.0%)와 숙박 체크인/아웃(95.2%)·객실유형(93.4%)·취사(69.9%)가 가장 뚜렷하다.** 쇼핑은 `saleitem`·`fairday`(장날)를 주지만 연결률이 6.6%라 실효 가치가 낮다 | **staging 보존 대상으로 기록한다.** `InfoKey` 매핑·등급화는 P2이며 여기서 결론 내지 않는다 |
| 2026-08-24 | `SHOP` 확장 후보 — 전국전통시장표준데이터 (`GO-80`) | data.go.kr 표준데이터 그리드에서 직접 다운로드해 실측 [측정: 부울경 356건 전량 × TourAPI 쇼핑(38) 부울경 1,555건 전량. 이름 정규화 정확 일치 70쌍 전량 좌표 거리 계산] | 신규 80.3%(285건), 일치 70쌍 전량 500m 이내(중앙값 22.8m)로 매칭 신뢰도 높음. 그러나 수정일 메타데이터가 단일 스냅샷(2025-11-26)이라 **최신성 미검증** | **확장 후보 자격은 있으나 채택 아님.** `other-source-survey.md` 1절 표·`source-downloads.md`에 반영. 재수집 간격을 두고 최신성을 측정해야 다음 단계로 갈 수 있다 |
| 2026-08-24 | `TOURIST_SPOT` 확장 후보 — 전국도시공원정보표준데이터 | data.go.kr에서 직접 다운로드 [측정: 부울경 2,563건 전량 × TourAPI 12+28 부울경 2,387건 전량. 이름 정규화 정확 일치 83쌍 전량 좌표 거리 계산] | 지번주소 99.6% vs 도로명주소 16.0% — 매칭 키 편차(L0 5절)가 다시 재현됐다. TourAPI 대비 이름 일치 3.2%지만 **좌표검증 500m 이내 64.3%뿐**(시장 원천 100%와 대비)이라 흔한 공원명 오매칭 위험이 크다. 어린이공원+소공원이 부울경의 71.9%로 관광 목적지형은 소수 | **확장 후보 자격은 있으나 채택 아님.** 이름 매칭만으로 신규를 확정하지 않는다 — 매칭 신뢰도가 낮은 원천은 신규율을 그대로 인용하지 않는다는 원칙을 추가한다 |
| 2026-08-24 | `GO-125` §8-3 미해결 — 15124679와 LOCALDATA 문화_테마파크업(기타)의 동일성 | data.go.kr에서 15124679 페이지를 직접 확인 [측정: 15124679 → 15045089 리다이렉트, 페이지 명시 전체 행 5,733 대 우리 4개 시도(부산·울산·경남·경북) 로컬 파일 합계 1,801행 재검산] | **확증됨.** 15124679/15045089는 별개 표준데이터가 아니라 우리가 이미 받은 LOCALDATA `문화_테마파크업(기타)`(2026-08-22 지역별 zip)의 data.go.kr 카탈로그 항목이다. 제공 URL도 `file.localdata.go.kr`을 그대로 가리키고, 좌표계도 표준데이터(WGS84)가 아니라 LOCALDATA 계열(EPSG:5174)로 판단된다 | **8-3의 보류를 해제한다.** 새 원본 다운로드 불필요. `ACTIVITY` 테마파크업 3종 판정(부울경 영업중 487, 채택 아님)을 그대로 유지 |
| 2026-08-24 | `ACCOMMODATION`/`ACTIVITY` 확장 후보 — 전국휴양림표준데이터 | data.go.kr에서 직접 다운로드 [측정: 부울경 44행 전량, 시설명 정규화 후 TourAPI 12+28 부울경 2,387건 대비 이름 일치 18쌍 좌표 거리 계산] | **44행 중 14건(31.8%)이 원천 자체 근접중복이다.** 같은 시설이 지역 접미사 유무로 두 번, 거제자연휴양림은 관리유형만 다르게 세 번 등록돼 있다. 고유 시설 30개 기준 TourAPI 대비 신규 40.0%(12개), 좌표 매칭 신뢰도는 높음(중앙값 23.1m). 지번주소 컬럼이 아예 없다(도로명만 제공) | **새 실패 유형으로 기록한다 — '행 존재 ≠ 고유 레코드'.** 지금까지의 '필드 존재 ≠ 사용 가능'·'값 단위 혼재'·'입력 칸 오용'·'극단적 이상치'에 이은 다섯 번째 유형. 이 원천을 쓰려면 정규화 후 dedup이 선행돼야 한다. 확장 후보 자격은 있으나 최신성 미검증으로 채택 아님 |
| 2026-08-24 | 축제 정보 보강 후보 — 전국문화축제표준데이터 (`GO-81`) | data.go.kr에서 직접 다운로드 [측정: 부울경 182건 전량의 `FSTVL_END_DATE`를 다운로드일(2026-08-24) 기준으로 대조. TourAPI 축제공연행사(15) 부울경 134건과 이름 정규화 대조] | **73.1%(133건)가 이미 종료일이 지난 축제**인데 삭제·갱신되지 않고 남아 있다. TourAPI 대비 이름 일치 22.0%로 명목상 신규 78.0%지만, 신규분 다수가 종료된 축제로 추정된다(세부 미분리) | **핵심 기준(최신성) 탈락으로 채택 아님을 실측으로 확정.** 기존 "TourAPI 925건 있어 후순위"라는 미실측 추정을 실측 결과로 대체했다 — 판단은 바뀌지 않았지만 근거가 바뀌었다 |
| 2026-08-24 | `TOURIST_SPOT`/`ACTIVITY` 정보 보강 후보 — 전국공공시설개방정보표준데이터 (8-5 신규) | data.go.kr에서 직접 다운로드 [측정: 부울경 1,134행 전수 필드 채움률·`OPEN_FCLTY_TYPE` 분포·`OPEN_LC_NM` 고유값] | 운영시간 채움률 100%로 정보는 풍부하나 **레코드 단위가 장소가 아니라 시설 내 개별 대관 공간**(1,134행 → 고유 시설 559개). 주류가 학교·공공기관의 세미나실·강당·운동장 대관이라 현재 subtype 체계에 대응 개념이 없다 | **판단 보류로 기록한다.** subtype 매핑은 P2 소관이라 이 세션에서 채택/탈락을 정하지 않는다. 연결성·최신성도 단위 문제 해소 전에는 재는 의미가 낮아 보류 |
| 2026-08-24 | 민간·계약형 후보 판정 범위 (`GO-125`, other-source-survey.md 1절) | Kakao Local·Google Places·Foursquare·Naver·Tripadvisor·예약 플랫폼(Booking/Agoda/Expedia)·OpenStreetmap의 P1 이용조건·품질을 전수 검토한 뒤, "P1 적재 부적합" 판정이 서비스 전체에서 이 원천들을 영구 제외하는지 확인 | 아니다 — P1 적재 판정(저장·재배포 가능한 원천을 staging에 쌓는 문제)과 서비스 전체 채택 판정(특정 기능에 필요한 실시간 조회·예약 연동을 붙이는 문제)은 별개다. 저장 제한 때문에 P1에서 제외된 원천도 실시간 조회형으로는 여전히 쓸 수 있다 | **공통 원칙으로 남긴다.** ① P1에서 적재 부적합 판정을 받은 민간 API가 서비스 전체에서 영구 제외되는 것은 아니다. ② 최신성이 중요한 사실(영업시간·폐업·가격·운영상태 등)이 필요해지면 실시간 조회형 원천을 다시 검토한다. ③ 예약·재고·가격 기능이 필요해지면 계약형 예약 원천을 다시 검토한다. ④ 재검토 시점에는 당시의 비용·약관·저장·캐싱·상업 이용 조건을 다시 확인한다(약관·요금은 시간에 따라 바뀐다 — Naver가 이번 세션 중 유료 전환된 사례 참고). ⑤ 실제 연동 구조와 provider 선택은 P3 또는 해당 기능 구현 단계에서 결정하며, 여기서 미리 설계하지 않는다 |
| 2026-08-25 | 원천별 종료 계약 (`GO-122`, L0 §4 "미관측 ≠ 종료") | TourAPI·상가정보가 인허가처럼 명시 종료 상태를 주는지 [측정: TourAPI 관광지(12) 2026-07-24 대비 2026-08-25 재수집 대조(전국 12,682→12,633, 사라짐 75건), 사라진 표본 3건 `detailCommon2` 개별 재조회로 실삭제 검증. 상가정보는 GO-120 실측 재확인] | 둘 다 목록/파일 스키마에 영업상태·삭제 플래그가 **없다**. TourAPI는 사라진 contentId가 `resultCode=0000`이지만 `totalCount=0`으로 응답해 진짜 삭제임을 확인했다 | **인허가형이 아니라 표준데이터형으로 확정.** 두 원천 모두 명시 종료 신호가 없어 미관측 임계값 설계가 필요한 부류로 분류한다. 임계값 자체는 `GO-124` 소관 |
| 2026-08-26 | P1 종료 판정 철회·재확정 — §1 A표 확장 후보 staging 누락 (`GO-125`) | 2026-08-25 phase gate 재판정("P1 실질 작업 완료")이 실제로 빠짐없는지 [측정: `data/raw` × `data/staging` × `other-source-survey.md` §1 A표 전수 대조] | **아니다 — §1 A표에서 "확장 후보 자격 있음"으로 실측까지 끝낸 17개 중 14개가 한 번도 staging되지 않았다.** 실측(측정·판정)과 staging(실제 적재)을 같은 것으로 착각한 것이 원인이다. 부수적으로 `STATUS_MAP`이 확장 LOCALDATA 업종의 상태 어휘(정상·정상영업·지정·승인·폐업처리·직권취소 등 다수)를 커버하지 못한 것도 함께 확인됐다 | **기존 "P1 데이터 정제 종료 가능" 판정을 철회하고, 상태 어휘 전수 측정 → `STATUS_MAP` 보완 → base 5종 regression 검증 → 남은 17개 전량 staging → 내부 중복(A/B/C/D) 판정·A만 dedupe → 완전성 재감사 순으로 다시 처리했다.** 17개 전량 staging 완료(raw→staging 전수 reconcile 일치), base 5종은 byte 단위로 무변경 확인, A_confirmed 96건 dedupe 적용(제거 101건, 그룹 안 both-included 기준). "실측 완료"와 "staging 완료"를 같은 문장에서 혼용하지 않는다는 원칙을 추가한다 |
